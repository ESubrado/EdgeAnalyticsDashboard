version: '3.8'

services:
  # Server (Node.js/Express)
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
      target: production
    container_name: edgeanalytics-server-prod
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env
    environment:
      NODE_ENV: production
    restart: unless-stopped
